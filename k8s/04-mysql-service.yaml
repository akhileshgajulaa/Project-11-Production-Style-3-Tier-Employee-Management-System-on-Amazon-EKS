apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: ems
  labels:
    app: mysql
spec:
  clusterIP: None   # headless — required for the MySQL StatefulSet
  selector:
    app: mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      protocol: TCP
